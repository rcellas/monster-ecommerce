import Header from "../components/Header/Header"

const Layout = () => {
  return (
    <div>
        <Header/>
        <main>

        </main>
        <footer>footer</footer>
    </div>
  )
}

export default Layout